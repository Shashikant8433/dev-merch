import React from "react";

function Logout() {
  return (
    <div
      style={{
        minHeight: "calc(100vh - 400px)",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Logout Bye!
    </div>
  );
}

export default Logout;
